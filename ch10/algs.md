```C++
<algorithm>
auto item = std::find(b, e, t);
int cnt = std::count(b, e, t);
bool flag = std::equal(b1, e1, b2);
std::fill(b, e, val);
std::fill_n(b, size, val);


<numeric>
int sum = accumulate(b, e, initSum);

```