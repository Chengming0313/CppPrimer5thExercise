#include <iostream>

using std::cin;
using std::cout;
using std::endl;

int main()
{
    // if (p = getPtr() != 0)
    // 赋值运算符的优先级太低 if ((p = getPtr()) != 0)

    // if (i = 1024)
    // if (i == 1024)

    return 0;
}