#include <iostream>
#include <string>

using namespace std;

/*
 * 输入运算符遇到第一个不为空白字符的字符开始读入，直到遇到下一个空白字符
 * getline(is, str)从遇到的第一个字符开始读取直到遇到换行符，
 * 它读取空白字符，但是不包括换行符，换行符读取出来后会被舍弃
 */
int main()
{
    
    return 0;
}